import Button from "@/components/Button";

const SortButton = () => {
  return (
    <Button
      icon="sort_by_alpha"
      Size="Small"
      Appearance="Tertiary"
      className="relative z-10"
    />
  );
};

export default SortButton;
